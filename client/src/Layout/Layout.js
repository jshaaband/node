import MenuBar from "../Menu/MenuBar";
import React from "react";

function Layout() {
  const value = {
    locale: "en",
  };

  return (
    <div>
      <MenuBar></MenuBar>
    </div>
  );
}

export default Layout;
