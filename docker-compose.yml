version: "3"
services:
  react-app:
    build:
      context: frontend
      dockerfile: frontend/Dockerfile.dev
    ports:
      - "80:80"
#    volumes:
#      - /app/node_modules
#      - .:/app
#  tests:
#    build:
#      context: frontend
#      dockerfile: frontend/Dockerfile.dev
#    volumes:
#      - /app/node_modules
#      - .:/app
#    command: ["npm","run","test"]
